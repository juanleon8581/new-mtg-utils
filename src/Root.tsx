import { Outlet } from "react-router-dom";
// import { Header } from "./presentation/components";

export const Root = () => {
  return (
    <>
      {/* <Header /> */}
      <Outlet />
    </>
  );
};
